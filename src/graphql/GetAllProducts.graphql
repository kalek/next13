query GetAllProducts {
	products {
		data {
			id
			name
			description
			price
			rating
			categories {
				id
				name
			}
		}
		total
	}
}

input ProductOrderByInput {
	name: Sort
	price: Sort
	avgRating: Sort
}

enum Sort {
	asc
	desc
}
